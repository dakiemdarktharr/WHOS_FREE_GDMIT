{
  "name": "@whos-free/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Who's free, gdmit — Phase 1 Express + Socket.io backend",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.7.0",
    "mongoose": "^8.16.0",
    "socket.io": "^4.8.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "socket.io-client": "^4.8.3",
    "tsx": "^4.20.0",
    "typescript": "^5.9.2"
  }
}
